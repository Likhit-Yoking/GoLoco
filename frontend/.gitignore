# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variable overrides
# .env          → committed (safe; holds dev defaults, no secrets)
# .env.production → committed (safe; values injected by CI at build time)
# .env.local      → NOT committed (developer-specific overrides)
.env.local
